Switch the current session to Plan Mode.

Use this when the task benefits from investigating and designing an approach before making changes. Plan Mode is read-only: continue using read and search tools, but do not modify files or run state-changing commands.

This tool never enables Yolo Mode.
