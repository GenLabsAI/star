Remove the git worktree created by `enter_worktree` and return to the original working directory.

No parameters required. The worktree associated with the current session is removed and git state is pruned.
